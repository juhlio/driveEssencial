{
    
    "routes": [{ "src": "/(.*)", "dest": "/app.js" }],
    "builds": [
      {
        "src": "/",
        "use": "@vercel/build-scripts",
        "config": {
          "buildCommand": "npm install && npm run build"
        }
      }
    ]
  }
  

  {
   
  }
  